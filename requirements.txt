# Runtime dependencies for the Streamlit app / HuggingFace Space.
# This is the inference subset of pyproject.toml — it intentionally omits the
# evaluation stack (ragas, mlflow, langchain-*), which the app does not import.
# For development install the full project instead: pip install -e ".[dev]"

streamlit>=1.40
pandas>=2.2
llama-index-core>=0.12
llama-index-vector-stores-lancedb>=0.3
llama-index-embeddings-huggingface>=0.4
lancedb>=0.13
sentence-transformers>=3.0
anthropic>=0.40
google-genai>=0.3
pymupdf>=1.24
beautifulsoup4>=4.12
pydantic>=2.9
python-dotenv>=1.0
